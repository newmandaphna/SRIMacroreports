# Web
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20

# Database. psycopg2-binary connects to Replit's built-in PostgreSQL.
# DATABASE_URL is injected automatically by the Replit environment.
sqlalchemy==2.0.37
alembic==1.14.1
psycopg2-binary==2.9.10

# Auth (Phase 1)
passlib[argon2]==1.7.4
argon2-cffi==23.1.0

# Google Sheets ingestion (Phase 2)
google-api-python-client==2.159.0
google-auth==2.37.0

# Dev and test
pytest==8.3.4
httpx==0.28.1
ruff==0.9.2
