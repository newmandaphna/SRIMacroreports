# SRI Practice Health Analytics -- dependencies
#
# The CORE pipeline (loaders, profiler, money, periods) uses the Python standard
# library ONLY. This is deliberate: money stays Decimal end-to-end (pandas would
# coerce money columns to float64 on read -- the silent-float bug ASSUMPTIONS §10
# forbids), and stdlib CSV keeps the Valant preamble/header handling transparent.

# Optional -- only needed to read .xlsx exports (CSV needs nothing extra):
openpyxl>=3.1

# Development / tests:
pytest>=7.0
