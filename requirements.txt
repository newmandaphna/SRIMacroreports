# Web
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
itsdangerous==2.2.0

# Database. sqlcipher3-binary ships a SQLCipher enabled SQLite, so the database file
# is encrypted at rest. See SECURITY.md section 5.2.
sqlalchemy==2.0.37
alembic==1.14.1
sqlcipher3-binary==0.5.4

# Auth (Phase 1)
passlib[argon2]==1.7.4
argon2-cffi==23.1.0

# Google Sheets ingestion (Phase 2)
google-api-python-client==2.159.0
google-auth==2.37.0

# Scheduling (Phase 2, default off)
apscheduler==3.11.0

# Dev and test
pytest==8.3.4
httpx==0.28.1
ruff==0.9.2
